# S7ABT Tweet Generation - Environment Variables
# These are set automatically when deployed to Lambda
# For local development, copy to .env and fill in values

# AWS Configuration
AWS_REGION=<your-aws-region>
ENVIRONMENT=dev

# Database (Secrets Manager ARN)
DATABASE_SECRET_ARN=arn:aws:secretsmanager:<your-region>:<account-id>:secret:s7abt/database/credentials

# Twitter API (Secrets Manager secret name)
TWITTER_SECRET_NAME=<your-twitter-secret-name>

# AWS Bedrock (for AI tweet generation)
BEDROCK_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-20250514-v1:0

# Lambda Configuration (automatically set in AWS)
AWS_NODEJS_CONNECTION_REUSE_ENABLED=1
