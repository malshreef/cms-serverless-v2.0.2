# S7ABT Frontend Environment Variables
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# API Configuration (from Public API deployment)
NEXT_PUBLIC_API_URL=https://<your-api-id>.execute-api.<your-region>.amazonaws.com/<stage>
NEXT_PUBLIC_SITE_URL=https://<your-domain>

# Tags API (same API Gateway, GetArticlesByTagid endpoint)
NEXT_PUBLIC_TAGS_API_URL=https://<your-api-id>.execute-api.<your-region>.amazonaws.com/<stage>/GetArticlesByTagid

# S3 Media Bucket URL (from Admin API SAM deploy output)
NEXT_PUBLIC_S3_BASE_URL=https://<your-s3-bucket>.s3.<your-region>.amazonaws.com

# MailerLite API (for newsletter/contact form - optional)
MAILERLITE_API_KEY=<your-mailerlite-api-key>

# AWS SES Configuration (for contact form email notifications - optional)
AWS_REGION=<your-aws-region>
AWS_ACCESS_KEY_ID=<your-aws-access-key-id>
AWS_SECRET_ACCESS_KEY=<your-aws-secret-access-key>

# Contact Form Settings (comma-separated list for multiple recipients)
CONTACT_NOTIFICATION_EMAILS=admin@your-domain.com
CONTACT_FROM_EMAIL=noreply@your-domain.com
